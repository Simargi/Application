const userList = 'https://reqres.in/api/users';
const countryList = 'https://restcountries.eu/rest/v1/all';
const companyHostData = 'https://connection.keboola.com/v2/storage';
const registerUsers = '';

export { userList, countryList, companyHostData, registerUsers }